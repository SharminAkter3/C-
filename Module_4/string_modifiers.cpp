#include <bits/stdc++.h>
using namespace std;
int main()
{
    // string a = "Hello";
    // string b = "A";
    // a += b;
    // a.append(b);
    // cout << a << endl;
    // cout << b << endl;
    // a = "HelloA"; -->works
    // a = a + b; --> works
    // a[5] = 'A'; ---> didn't works
    // a.push_back('A'); // works
    // a.pop_back();
    // a.pop_back();
    // cout << a << endl;

    // a = "Hiii";
    // a.assign("HIII");

    string a = "HelloWorld";
    // a.erase(4, 1);
    // a.replace(4, 3, "hii");
    // a.insert(5, "Sharmin");
    cout << a << endl;
    return 0;
}